#ifndef AUDIO_CAPTURE_ALSA_H
#define AUDIO_CAPTURE_ALSA_H

#include <alsa/asoundlib.h>

#include <audio-capture.h>

struct audio_capture_alsa {
};

extern struct audio_capture_interface audio_capture_interface_alsa;

#endif
